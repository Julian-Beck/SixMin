import { View } from "react-native";

type CasinoProps = {
  count: number,
};

export default function Casino({
  count
}: CasinoProps) {
    return (
        <View>
            
        </View>
    );
}
